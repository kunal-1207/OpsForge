apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: opsforge-api-pod-kill
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    labelSelectors:
      app: api-service
  duration: "1m"
  scheduler:
    cron: "@every 2m"
